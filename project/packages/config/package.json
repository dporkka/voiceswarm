{
  "name": "@aasop/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configuration for TypeScript, ESLint, and Prettier",
  "main": "./tsconfig/base.json",
  "files": ["tsconfig", "eslint", "prettier"],
  "scripts": {
    "clean": "rm -rf .turbo"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
